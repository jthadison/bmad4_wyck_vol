# Production Environment Configuration
# For local Docker production testing, use localhost URLs
# For VPS deployment (Phase 2), update with production domain

# REST API Base URL
VITE_API_BASE_URL=http://localhost:8000/api/v1

# WebSocket Base URL
VITE_WS_BASE_URL=ws://localhost:8000/ws

# Application Version
VITE_APP_VERSION=0.1.0

# Sentry DSN (error tracking - deferred to Phase 2)
# VITE_SENTRY_DSN=
