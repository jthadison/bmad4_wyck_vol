# Production Environment Configuration
# API calls go through nginx proxy (/api → backend:8000)
# WebSocket goes through nginx proxy (/ws → backend:8000/ws)

# REST API Base URL - relative path, proxied by nginx
VITE_API_BASE_URL=/api/v1

# WebSocket Base URL - leave empty to use smart detection (auto wss:// from page protocol)
VITE_WS_BASE_URL=

# Application Version
VITE_APP_VERSION=0.1.0

# Sentry DSN (error tracking - deferred to Phase 2)
# VITE_SENTRY_DSN=
